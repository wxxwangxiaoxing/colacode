# 服务特定配置（数据源、Redis、Sa-Token 已从 Nacos 共享配置加载）
server:
  port: 3015

spring:
  application:
    name: colacode-interview

interview:
  ai:
    enabled: false
    api-key: ""
    service-name: external-ai-service
    base-url: ""
